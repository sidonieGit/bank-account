spring:
  mongodb:
    uri: ${SPRING_DATA_MONGODB_URI}   # no default — must be set on the platform (Render env var)
    auto-index-creation: false

management:
  endpoint:
    health:
      show-details: never
      show-components: never
